name: Temp patch account create v2
on:
  push:
    branches: [main]
    paths: ['.github/workflows/temp-patch-account-create-v2.yml']
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }
      - name: Restore UUID-safe worker account creation
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/use_worker_for_account_create.py
          node --check web-admin/src/backend-runtime.js
          grep -q "text/plain;charset=UTF-8" web-admin/src/backend-runtime.js
          grep -q "initial_password: body.initial_password || body.password || ''" web-admin/src/backend-runtime.js
          ! grep -q 'api_admin_create_profile_rpc' web-admin/src/backend-runtime.js
          grep -q 'DỮ LIỆU THEO NGÀY' web-admin/src/ops-console.js
          ! grep -q "opsStaffSync').onclick" web-admin/src/ops-console.js
      - name: Commit Web patch
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add web-admin/src/backend-runtime.js
          git diff --cached --quiet || git commit -m 'fix(web): use UUID-safe Firebase Admin worker for account creation'
          git pull --rebase origin main
          git push origin HEAD:main
